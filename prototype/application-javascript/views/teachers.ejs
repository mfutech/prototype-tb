<!DOCTYPE html>
<html>

<head>
    <%- include('partials/head') %>
</head>

<body>
    <%- include('partials/menu') %>

    <div class="container">
        <h1 class="py-5">Teachers</h1>

        <table class="table">
            <thead class="thead-dark">
                <tr>
                    <th scope="col">First name</th>
                    <th scope="col">Last name</th>
                    <th scope="col">Email</th>
                    <th scope="col"></th>
                </tr>
            </thead>
            <tbody class="table-striped">
                <% for(var i = 0; i < teachers.length; i++) { %>
                <tr>
                    <td><%= teachers[i].firstname %></td>
                    <td><%= teachers[i].lastname %></td>
                    <td><%= teachers[i].username %></td>
                    <td><a href="/teachers/delete/<%= teachers[i].username %>" class="btn btn-outline-danger float-right">Remove</a></td>
                </tr>
                <% } %>
            </tbody>
        </table>

        <a class="btn btn-primary float-right mt-5" href="/teachers/add" role="button">Add teacher</a>
    </div>
    
    <%- include('partials/script') %>
</body>

</html>